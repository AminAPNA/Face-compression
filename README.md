# Face-compression
An image is a grid of pixels, each representing a color in RGB format with values 0–255, requiring 24 bits (3 bytes) per pixel. Full HD images (1920×1080) need ~6 MB. This project explores compressing facial images efficiently while preserving features for recognition.
